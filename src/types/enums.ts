export enum TransactionType {
    SWAP = 'SWAP',
    TRANSFER = 'TRANSFER'
}

export enum TokenStandard {
    SPL = 'SPL'
}

export enum LogLevel {
    DEBUG = 'DEBUG',
    INFO = 'INFO',
    WARN = 'WARN',
    ERROR = 'ERROR'
}
